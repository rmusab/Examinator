unit Loading;

interface

uses
  Windows, Messages, SysUtils, Variants, Classes, Graphics, Controls, Forms,
  Dialogs, ExtCtrls, StdCtrls, RzLabel, RzBckgnd, jpeg;

type
  TForm5 = class(TForm)
    Timer1: TTimer;
    Background: TRzBackground;
    Separator2: TRzSeparator;
    Separator1: TRzSeparator;
    lbVerion_nb: TRzLabel;
    lbVersion: TRzLabel;
    lbFreeware: TRzLabel;
    lbLicense: TRzLabel;
    lbDeveloper: TRzLabel;
    lbName: TRzLabel;
    lbCopy: TRzLabel;
    lbEmail: TRzLabel;
    lbMail: TRzLabel;
    lbProduct: TRzLabel;
    procedure Timer1Timer(Sender: TObject);
  private
    { Private declarations }
  public
    { Public declarations }
  end;

var
  Form5: TForm5;

implementation

{$R *.dfm}

procedure TForm5.Timer1Timer(Sender: TObject);
begin
  Timer1.Enabled := false;
end;

end.
